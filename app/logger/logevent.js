const eventType = {
  login: 'Login_Event'.toUpperCase(),
  createFile: 'Create_File_Event'.toUpperCase(),
  uploadFile: 'Upload_File_Event'.toUpperCase(),
  runProcessModel: 'Run_Process_Model_Event'.toUpperCase(),
  deleteCase: 'Delete_Case_Event'.toUpperCase(),
  createCase: 'Create_Case_Event'.toUpperCase(),
};
export default eventType;
